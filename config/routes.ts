const theme = 'default'

const routes = [
    {
        path: "/install",
        component: '@/install',
    },
    {
        path: '*',
        component: '@/routes',
    },
]

export default routes